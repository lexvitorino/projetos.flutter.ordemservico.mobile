import 'package:flutter/material.dart';

class ServiceOrderPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("ServiceOrderPage"),
      ),
      body: Text("ServiceOrderPage"),
    );
  }
}
